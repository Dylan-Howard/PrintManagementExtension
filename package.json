{
  "name": "wcps-management-kit",
  "version": "1.0.0",
  "description": "Management kit for WCPS devices",
  "main": "hello.html",
  "scripts": {
    "compile": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "gts lint",
    "clean": "gts clean",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@tsconfig/recommended": "^1.0.8",
    "chrome-types": "^0.1.325",
    "typescript": "^5.6.3",
    "gts": "^6.0.2",
    "@types/node": "^22.7.5"
  }
}
